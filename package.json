{
    "name": "rokumio-client",
    "version": "0.1.0",
    "private": true,
    "description": "Native Roku client for Stremio addons and debrid streams",
    "scripts": {
        "build": "bsc",
        "clean": "node -e \"require('fs').rmSync('dist', { recursive: true, force: true }); require('fs').rmSync('out', { recursive: true, force: true })\""
    },
    "devDependencies": {
        "brighterscript": "^0.73.1"
    }
}
