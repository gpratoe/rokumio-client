{
    "name": "rokumio-client",
    "version": "0.1.0",
    "private": true,
    "description": "Native Roku client for Stremio addons and debrid streams",
    "scripts": {
        "build": "bsc",
        "test": "node tests/run.js && node tools/mock-addon-server/server.test.js",
        "clean": "node -e \"require('fs').rmSync('dist', { recursive: true, force: true }); require('fs').rmSync('out', { recursive: true, force: true }); require('fs').rmSync('out-test', { recursive: true, force: true })\""
    },
    "devDependencies": {
        "@rokucommunity/brs": "^0.47.4",
        "brighterscript": "^0.73.1"
    }
}
